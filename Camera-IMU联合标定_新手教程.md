# Camera-IMU 联合标定 · 新手教程

> 面向：RoboMaster 算法组预备队员（中国科大 **RoboWalker** 战队）
> 对应任务书：[算法组新生提前批大作业：Camera-IMU 联合标定](https://git.lug.ustc.edu.cn/water5/rw-algorithm-train-2027/-/blob/main/task/calibration/%E7%AE%97%E6%B3%95%E7%BB%84%E6%96%B0%E7%94%9F%E6%8F%90%E5%89%8D%E6%89%B9%E5%A4%A7%E4%BD%9C%E4%B8%9A%EF%BC%9ACamera-IMU%20%E8%81%94%E5%90%88%E6%A0%87%E5%AE%9A.md)
> 硬件状态与实测数据来自 2026-09-13 现场核对，命令均可直接复现。

---

## 0. 这份教程怎么用

这不是"标定算法综述"，而是一份**从你现在的位置走到验收台前**的路线图。三件事：

1. **第 2 章**把任务书逐条翻译成"你要交付什么、验收官会怎么查"——先看这个，避免做完了发现方向错了。
2. **第 4 章**只讲你真正用得上的概念，每个概念都配"所以你在代码里要做什么"。
3. **第 5 章**是分阶段路线图，每阶段有明确的**完成判据**。建议按 C0→C4 推进，不要跳。

第 3 章是**你手上这台机器的真实状态**，第 7 章是我实际踩出来的坑。

---

## 1. 你在哪：赛事、队伍、这道题的位置

### 1.1 背景

RoboMaster 机甲大师赛是面向高校的机器人对抗赛。中国科大 **RoboWalker** 战队在
2025 赛季拿到[中部赛区冠军](https://zsb.ustc.edu.cn/_t4927/2025/0603/c35498a686805/page.psp)
和[全国赛亚军](https://zsb.ustc.edu.cn/2025/0907/c35498a699127/page.htm)。

算法组（视觉组）在场上要解决的核心问题，抽象成一句话就是：

> **把"图像里的某个东西"变成一个"云台可以执行的、带物理单位的指向"。**

这个链条是：

```
图像像素  ──① 检测/识别──▶  像素坐标
                              │
                              ▼  ② 相机内参 + 去畸变
                         相机系下的方向向量
                              │
                              ▼  ③ Camera-IMU 外参（旋转 R_IC）
                        IMU / 云台系下的方向
                              │
                              ▼  ④ 弹道解算、时间补偿
                          云台目标角度
```

你们的这道大作业，做的正是 **② 和 ③** 这两步的基础设施。

### 1.2 为什么用这道题筛人

因为它同时考四件事，而且缺一样就做不出来：

| 考点 | 具体体现 |
|---|---|
| **数学功底** | 坐标系变换方向、旋转表示、手眼标定 AX=XB 的可解性 |
| **动手能力** | 硬件接线、驱动、权限、时间戳、数据采集 |
| **工程习惯** | 参数不能硬编码、结果要能复现、要能解释物理意义 |
| **诚实与自省** | 结果验证 —— 不是"跑出来一个数"，而是"凭什么相信这个数" |

第 4 项是很多新人的**致命短板**：交一份漂亮的 `T_IC` 却说不清对错，比交一个
明确标着"误差偏大、原因可能是 X"的结果要差得多。

---

## 2. 任务书翻译：从"要求"到"我要交付什么"

### 2.1 逐条对照表

| 任务书原文 | 你要实现的东西 | 验收官会怎么查 |
|---|---|---|
| 2.1 相机数据：图像 + 每张图的时间戳 | 采集程序，存 `images/000000.png…` + 一份时间戳文件 | 看你的数据目录里有没有时间戳，格式能否被程序读回 |
| 2.2 IMU 数据：`timestamp, gx gy gz, ax ay az` | 串口解析 + 落盘 | 拿一组新数据跑，看能否正确读出 |
| 2.3 标定板参数：点数、间距、实际尺寸 | 配置文件里的 `target` 段 | **改一下配置文件里的 `square_size`，看结果是否跟着变** |
| 3.1 相机内参 fx fy cx cy + 畸变 | 内参标定模块 | 看输出的数是否落在合理范围（见 5.2） |
| 3.2 Camera-IMU 外参 R_IC, t_IC | 手眼标定模块 | **问你 `T_IC` 是哪个系到哪个系**（必问） |
| 3.3 结果保存成文件且能重新读回 | `results/*.yaml` + `*.json` | 让你现场写个读取脚本 |
| 4. 结果验证（至少一种） | 重投影误差 / 运动一致性 / 多次标定对比 | 让你解释"这个数多少算好、为什么" |
| 5. 输入路径和板参数不硬编码 | 配置文件 / 命令行参数 | 改配置重跑，看是否需要动代码 |
| 6. README 说明环境/依赖/编译/运行/格式/坐标系 | README | 照 README 从零跑一遍 |
| 7. 验收 8 条 | 见下 | 逐条过 |

### 2.2 三个最容易丢分的点

**① 坐标系方向说不清（对应验收第 8 条）**

任务书要求"必须在文档中明确说明 `T_IC` 是哪一个坐标系到哪一个坐标系的变换"。
这不是形式主义——**这是整个任务里最容易被误解、也最容易被追问的一点**。
详见 4.1。

**② 只输出结果，不做验证（对应第 4 条）**

"标定残差、重投影误差、运动一致性、多次对比、与成熟工具对比"——**至少一种**，
但请至少做两种，并在报告里写清："指标 X 达到阈值 Y，所以我判断结果合理；
不达标的话最可能的原因是 Z"。

**③ 采集动作不合格（最隐蔽）**

标定精度 **90% 取决于采集动作**，不是算法。只绕一个轴转 → 手眼标定数学上无解；
只旋转不平移 → 平移外参不可观测。参考 4.5 和 5.3。

---

## 3. 你的硬件现状（2026-09-13 实测）

### 3.1 清单

| 设备 | 型号 | 接口 | 状态 |
|---|---|---|---|
| **IMU 板** | `H7_IMU_With_EKF`（PID `0483:6666`） | CDC 串口 `/dev/ttyACM0` | ✅ **完全正常**，协议已逆向后全部验证 |
| 工业相机 | 海康 `MV-CS020-10UC` | USB3 Vision（非 UVC） | ⚠️ 能枚举、能读参数，**取流未打通** |
| 笔记本摄像头 | ASUS FHD webcam | UVC `/dev/video0` | ✅ 640×480@30fps 可用 |
| 算力 | Ubuntu 24.04 / Python 3.12 / ROS 2 Jazzy / OpenCV 4.6 | — | ✅ 够用 |

### 3.2 IMU：这块板已经彻底搞定

**协议结论：82 字节定长帧，1 kHz，小端，帧头 `5A A5`。**

| 偏移 | 字段 | 单位 |
|---|---|---|
| 0 | `5A A5` 帧头 | — |
| 14 | `t_board_ms`（板载毫秒，每帧 +1） | ms |
| 18 / 22 / 26 | `ax ay az` | m/s² |
| 30 / 34 / 38 | `gx gy gz` | rad/s |
| 42 / 46 / 50 | `mx my mz` | **恒 0，磁力计未启用** |
| 54 / 58 / 62 | `roll pitch yaw` | rad |
| 66 / 70 / 74 / 78 | `qw qx qy qz`（**w 在前**） | — |

实测体检（3 秒，3001 帧）：

```
采样率          999.9999999999999 Hz
时间戳步长非1的帧数   0            ← 零丢帧
四元数模长最大偏差    1.3e-07
四元数vs欧拉角最大误差 2.4e-07 rad  ← 字段解释正确
加速度模长均值       9.7667 m/s²   ← 静止，≈ 重力
```

**两个直接可用的结论：**

1. **`az ≈ +9.77` 说明这块板的 z 轴朝上**（静止时加速度计测的是比力，
   朝上的轴读 +g）。这直接决定了你的 IMU 系定义，写进文档。
2. **做时间对齐时，优先用 `t_board_ms`，不要用主机收到数据的时刻。**
   板载 1 kHz、单调、零丢帧；USB 到达时刻有几十毫秒的抖动。

完整证据链与复现方法见 `~/calib_ws/doc/H7_IMU协议.md`。

> ⚠️ 一个真实踩过的坑：早期用 `cat /dev/ttyACM0` 抓包，帧长看起来在 76~100 字节
> 之间抖动，差点被当成"变长协议"。真实原因是 **tty 行规程篡改/吞掉了字节**。
> **二进制串口协议绝对不要用 `cat` 或 `readline()` 调试**，要用 pyserial
> （或先 `stty -F /dev/ttyACM0 raw`）。

### 3.3 相机：两条路，先走能走通的那条

海康 `MV-CS020-10UC` 三个接口全是 `bInterfaceClass=239 / SubClass=5`（USB3 Vision），
**没有 UVC 接口，所以 OpenCV 和 `/dev/video*` 永远打不开它**——这不是配置问题，是协议不同。

当前 aravis 的实际表现（比之前有进展，但仍不稳）：

```
Found 1 device
Testing 'U3V:MV-CS020-10UC'
Properties:SensorSizeReadout     SUCCESS   ← GenICam 属性读得到
Properties:ExposureTimeReadout   SUCCESS
MultipleAcquisitionA:BufferCheck FAILURE 0/10  USB3Vision write_memory timeout  ← 取流失败
```

而且**同一分钟内重跑会退回到 `Failed to connect`**。寄存器读写时好时坏 + 取流超时，
典型指向 **USB3 链路质量问题**（线缆 / 供电 / 主控口），而非纯软件不兼容。

**建议顺序（成本从低到高）：**

1. **换线**：用相机原装 USB3 线，或明确标 5 Gbps 的短线。很多"随机超时"就是线。
2. **换口**：直接插机身 USB3 口，不要过扩展坞。
3. **核对链路速率**：`lsusb -t | grep -B1 -A3 2bdf`，必须是 `5000M`；
   若变成 `480M` 就是线/口退化了。
4. 都排完仍不行 → 上海康 **MVS SDK**（官方路线，且提供**硬件时间戳**，
   对 Camera-IMU 标定的时间对齐有实质收益）。

**关于时间成本的重要建议：不要等相机。**

标定算法对分辨率不敏感。**先用笔记本摄像头（`/dev/video0`，现在就能用）把
C1→C4 全部跑通**，等 MVS 装好只需换数据源，算法零改动。这是最省时间的路径。

### 3.4 你现在可以立刻做的三件事

```bash
# ① 验 IMU 还活着（3 秒，打印协议体检表）
cd ~/calib_ws
python3 calib/imu_h7.py --port /dev/ttyACM0 --seconds 3

# ② 采一组真实数据（相机 + IMU，无窗口自测）
.venv/bin/python tools/capture_h7.py --out /tmp/selftest --frames 3

# ③ 把 IMU 稳定别名修好（需要你自己的终端输 sudo 密码）
sudo bash ~/calib_ws/tools/setup_permissions.sh   # 脚本已更新，含新板 PID
#    之后可以用 /dev/rw_imu 代替 /dev/ttyACM0
```

---

## 4. 核心概念速成

### 4.1 坐标系与变换方向（最容易翻车，必读）

**先把三个系写死，写进 README，一个字都不要含糊：**

| 系 | 定义 |
|---|---|
| `C` 相机系 | 原点在光心；**x 向右、y 向下、z 沿光轴向前**（OpenCV 约定） |
| `I` IMU 系 | 原点在 IMU 测量中心；三轴按板子丝印。**你这块板 z 轴朝上**（实测 `az≈+9.77`） |
| `B` 标定板系 | 原点在棋盘格左上角第一个内角点；z 垂直板面向外 |

**约定：**

$$T_{IC}=\begin{bmatrix}R_{IC}&t_{IC}\\0&1\end{bmatrix},\qquad p_I = R_{IC}\,p_C + t_{IC}$$

即 **`T_IC` 把相机系下的点变到 IMU 系下**（"相机 → IMU"）。

**为什么这里必须较真：**

- 业界两种写法都有人用。**Kalibr 输出的 `T_cam_imu` 语义是反的**（IMU → 相机，
  即 `T_CI`）。你要么对齐 Kalibr，要么明确声明，**但不能不说话**。
- 写反了的后果：旋转差一个转置，平移方向整个反过来。数值上"看起来也很正常"，
  所以**不会报错，只会悄悄错**。

**自检方法（强烈建议写成单元测试）：**

```python
# T_CI 必须是 T_IC 的逆，且 R_CI == R_IC.T
T_CI = np.linalg.inv(T_IC)
assert np.allclose(T_CI[:3, :3], T_IC[:3, :3].T)
assert np.allclose(T_CI[:3, 3], -T_IC[:3, :3].T @ T_IC[:3, 3])
```

**再加一道物理自检**：把 IMU 测到的重力方向投到相机系，看它是否和相机看到的
标定板法向 / 场景地平线自洽。数值对不上就是方向错了。

### 4.2 针孔相机模型与畸变

$$\begin{bmatrix}u\\v\\1\end{bmatrix}\sim K\begin{bmatrix}X/Z\\Y/Z\\1\end{bmatrix},\quad K=\begin{bmatrix}f_x&0&c_x\\0&f_y&c_y\\0&0&1\end{bmatrix}$$

畸变用 **plumb_bob（Brown-Conrady）** 就够：

$$x_d = x(1+k_1r^2+k_2r^4+k_3r^6)+2p_1xy+p_2(r^2+2x^2)$$

**你要输出**：`fx, fy, cx, cy, [k1, k2, p1, p2, k3]`。

**合理性判断**（写进报告里）：

- `cx, cy` 应接近图像中心 `(W/2, H/2)`，偏差几个像素到几十像素都正常；
- `fx, fy` 应接近且量级合理（640×480 的普通摄像头大概 500~800 像素）；
- `|fx - fy| / fx` 一般 < 1%（像素不是正方形才会有差异）；
- `k1` 通常是负的（桶形畸变），量级 1e-2 ~ 1e-1。

### 4.3 棋盘格内参标定到底在干什么

**一句话**：已知一堆 3D 点（棋盘角点在板坐标系下，z=0 平面上，间距 = `square_size`）
和它们在图像里的 2D 位置，求 `K` 和畸变系数。

**Zhang 的方法（OpenCV `calibrateCamera` 就是它）：**

1. 每个姿态下，角点的 3D-2D 对应给出一个**单应矩阵 H**；
2. 由 H 的约束（旋转矩阵列向量正交且等长）解出 `K` 的初值；
3. 用 Levenberg-Marquardt 做**最大似然细化**，同时优化 `K`、畸变、每个姿态的外参。

**你需要知道的四件事：**

1. **`square_size` 写错 → 内参仍然对，但所有平移的尺度整体错。**
   这是任务书 5.3 特别点出的。打印棋盘格后**用尺子量**，别信"应该是 25 mm"。
2. **姿态多样性比帧数重要。** 20 张覆盖四角+不同倾角的图，胜过多 100 张都在画面中央。
   只在一个平面/一个距离上采集 → `fx` 与深度耦合，标不准。
3. **角点要做亚像素细化**（`cv2.cornerSubPix`），粗检测的重投影误差能差一个量级。
4. **不要用同一批数据既标定又评估误差**——那叫自证，结果必然乐观。
   留出 20% 的图做验证集，用**它们**算重投影误差。

### 4.4 IMU：你拿到的是什么

这块板给的 6 个通道：

- **加速度计**测的是**比力**（specific force），不是"加速度"。
  静止时读数 ≈ 重力反方向，模长 ≈ 9.8 m/s²。所以静止时 `az≈+9.77`
  说明 z 轴朝上——**这也是你确定 IMU 轴方向最直接的办法**。
- **陀螺**测角速度，静止时读数就是**零偏（bias）**。你的板子静止时约
  `0.004 rad/s ≈ 0.23 °/s`，**标定前必须减掉**，否则积分一分钟就漂 14°。
- **磁力计恒为 0**，所以 **yaw 不可观测**。别设计依赖磁力计的方案。

**静止段是你最好的朋友。** 采集时开头留 2 秒完全静止：
- 减零偏：取静止段陀螺均值；
- 对齐重力：取静止段加速度均值，得到 IMU 系的"上"方向，用来构造初始姿态。

**要往深走**：Allan 方差可以标出噪声密度和随机游走（配置文件里
`gyro_noise_density` 那几项就是为它准备的）。作业层面用静止段估个标准差就够。

### 4.5 Camera-IMU 外参为什么能解出来

**物理事实**：相机和 IMU **机械固连**，所以它们之间的变换 `T_CI` 是**常数**。

**推理（手眼标定 AX = XB）：**

设标定板固定不动，相机+IMU 刚性体一起运动：

- 相机看到板 → 得到 **板在相机系下的位姿** $P(t) = T_{CB}(t)$（`solvePnP` 直接给）；
- IMU 积分 → 得到 **IMU 相对世界的姿态** $T_{WI}(t)$；
- 因为 `T_CI` 和板在世界中的位姿都是常数，两个相邻时刻消元后得到：

$$\underbrace{T_{WI}(t_2)^{-1}T_{WI}(t_1)}_{A}\;X \;=\; X\;\underbrace{P(t_2)P(t_1)^{-1}}_{B}$$

其中 $X = T_{CI}$。这就是经典的 **$AX=XB$**，有成熟解法
（Tsai-Lenz / Park-Martin / Daniilidis 对偶四元数）。

**它的可解性有硬性条件**（这就是"采集动作要求"的数学来源）：

| 条件 | 不满足会怎样 |
|---|---|
| 运动要绕**至少两个不平行的轴** | `A` 的旋转轴全平行 → 旋转外参不可观测 → **无解** |
| 要有**平移激励** | 纯旋转 → `t_IC` 完全不可观测 → 平移解出来是噪声 |
| 角速度要足够大 | 信噪比不足 → 解病态 |

**作业层面的简化路线（推荐先做这个）：**

不一定要做完整 AX=XB。更简单且足够交差的是**运动相关法**：

1. 用 PnP 得到相邻帧之间相机的**相对旋转** $\Delta R_{cam}$；
2. 用陀螺积分得到相邻帧之间 IMU 的相对旋转 $\Delta R_{imu}$；
3. 因为 $R_{IC}\Delta R_{cam} = \Delta R_{imu} R_{IC}$（同样是 AX=XB 的旋转部分），
   用**足够多**的样本最小二乘解出 $R_{IC}$；
4. 平移用带杠杆臂的关系：$t$ 与角速度、加速度的耦合解出。

先跑通这条，再考虑上 Ceres 做全局优化。

### 4.6 时间对齐：比算法更容易毁掉结果

**这一节的重要性被严重低估。**

IMU 在 1 kHz、相机在 30 fps，而且两者在**不同的时钟**上：

- IMU 时间戳 = `t_board_ms`（板载 1 kHz 计数）
- 相机时间戳 = 主机单调时钟（`time.monotonic()`）

它们之间有一个**未知常量偏移** $\tau$。

**误差有多致命**：假设云台以 90 °/s 转动，时间戳差 10 ms：

$$90\,°/s \times 0.01\,s = 0.9°$$

**0.9° 的姿态误差，直接毁掉外参标定的精度**（验收标准里旋转误差要求 < 1~2°）。

**怎么估 $\tau$（从易到难）：**

1. **相关法（推荐）**：把陀螺模长 $\|\omega_{imu}\|$ 与由相邻相机位姿算出的
   角速度大小 $\|\omega_{cam}\|$ 做互相关，峰值位置就是 $\tau$。
2. **冲击法**：手持刚性体**猛地顿一下**（产生一个尖锐的加速度尖峰），
   在 IMU 里找尖峰、在图像序列里找运动突变的那一帧，两者时间差即 $\tau$。
3. **联合优化**：把 $\tau$ 作为一个待估参数丢进优化，和外参一起解。

**顺手能做的两个加分项**：

- 采集时**锁曝光**（`tools/capture_h7.py --lock-exposure`）：自动曝光会让帧率抖动，
  时间戳就不可靠了；而且运动模糊会降低角点精度。
- 把 `t_board_ms` 和主机时刻**一起存下来**（`imu_h7_full.csv` 里都有），
  这样 $\tau$ 可以事后估，不用重采。

---

## 5. 分阶段路线图

> 每个阶段都给了**完成判据**。没达到判据就别进下一阶段——否则错误会一路传播，
> 最后你根本不知道是哪一步错了。

### C0 · 环境与数据链路

**做什么**

1. 把 `~/calib_ws` 跑起来（虚拟环境已建好，见其 README 第 3 节）。
2. 跑通 IMU 协议自检和采集自测（第 3.4 节的命令）。
3. 修好 `/dev/rw_imu` 别名（跑一次 `setup_permissions.sh`）。
4. 决定相机路线：**先用笔记本摄像头**，海康相机并行排查（3.3 节）。

**完成判据**

- [ ] `python3 calib/imu_h7.py --seconds 3` 输出采样率 ≈ 1000 Hz、`时间戳步长非1的帧数 = 0`
- [ ] `tools/capture_h7.py --frames 3` 能在输出目录里同时产出图像、时间戳、`imu.txt`
- [ ] 你**能解释** `imu.txt` 里每一列是什么、什么单位、来自哪个时钟

### C1 · 相机内参

**做什么**

1. 打印/准备标定板，**用尺子量出真实 `square_size`**，写进 `config/calibration.yaml`。
2. 采集 20~30 张：覆盖画面**四角 + 中心**，每张给不同**倾角**，不同距离。
3. 实现 `calib/intrinsics.py`：`findChessboardCorners` → `cornerSubPix`
   → `calibrateCamera`。
4. 划出 20% 作为验证集，**只在验证集上**算重投影误差。

**完成判据**

- [ ] 验证集重投影误差 RMS **< 0.5 px**（< 1.0 px 可接受）
- [ ] `cx, cy` 接近图像中心，`fx ≈ fy`
- [ ] 去畸变后的图像里，**直线是直的**（把一张棋盘格图去畸变后肉眼看）

### C2 · 时间对齐

**做什么**

1. 采集一段**缓慢、持续、绕单轴**的转动数据（角速度 30~90 °/s）。
2. 用 PnP 得到每帧相机姿态；用陀螺积分得到 IMU 姿态。
3. 用互相关估出时钟偏移 $\tau$，写进结果文件。

**完成判据**

- [ ] $\tau$ 的估计在不同数据段上**稳定**（重复性优于几毫秒）
- [ ] 把 $\tau$ 补偿后，相机角速度与陀螺角速度的波形**肉眼对齐**

### C3 · 外参标定

**做什么**

1. 采集**合格的运动激励**（这是本阶段成败关键）：
   - 开头 **2 秒完全静止**（零偏 + 重力对齐）；
   - 绕 **x / y / z 三个轴分别**缓慢转 2~3 圈；
   - 做**小幅平移**（前后左右上下各几次）；
   - 全程标定板在画面内、相机与 IMU **相对位置绝对不变**。
2. 实现 `calib/extrinsics.py`：先做运动相关法解 $R_{IC}$，再解 $t_{IC}$。
3. 输出 `T_IC`，**在结果文件里写清方向约定**。

**完成判据**

- [ ] 运动激励体检通过：`tools/check_motion.py` 显示三个轴的角速度都有可观激励
- [ ] $R_{IC}$ 转成的欧拉角**物理上说得通**（相机相对 IMU 的安装角，通常很小）
- [ ] $\|t_{IC}\|$ 与**用尺子量的**相机光心到 IMU 中心的距离**量级一致**（±几厘米）
- [ ] 平移部分**不是**在纯旋转数据上解出来的（那必然是噪声）

### C4 · 验证与报告

**做什么**（至少做两种，建议全做）

| 方式 | 指标 | 合格参考 |
|---|---|---|
| 重投影误差 | 验证集角点 RMS | < 0.5 px 好 / < 1.0 px 可接受 |
| **运动一致性** | 由相机和 IMU 各自推算的姿态差角 | 均值 < 1~2° |
| 多次独立标定 | 数据切 3 份分别标定，比外参离散度 | 旋转标准差 < 1°，平移 < 数 mm |
| 与成熟工具对比 | 与 Kalibr 结果比差角 | 差角 < 1~2° |

**完成判据**

- [ ] 报告里回答了："这个指标多少算好" + "我的结果是否达到" + "不达标的原因是什么"
- [ ] 验收 8 条逐条能演示

---

## 6. 工程规范与提交清单

```
项目目录/
├── README.md              ← 环境/依赖/安装/运行/输入格式/输出格式/坐标系定义
├── calib/                 ← 源代码
├── config/calibration.yaml← 所有参数（不硬编码！）
├── tools/                 ← 采集、自检、体检脚本
├── data/                  ← 示例输入数据（哪怕只放 3 帧 + 1 秒 IMU）
└── results/               ← 示例标定结果 + 验证报告
```

**几条会让你加分的工程习惯：**

1. **配置文件里写 `convention: T_IC_means_camera_to_imu`**，并在结果文件里
   原样回写。这样验收官永远不会误解你的方向。
2. **结果文件同时存 YAML 和 JSON**，并**每次运行存一份带时间戳的快照**——
   "多次独立标定对比"这个验证方式就免费得到了。
3. **每个数字都带单位**（`fx: 610.3  # px`）。
4. **数据目录里放一份最小可跑通的示例数据**（3 张图 + 1 秒 IMU），
   验收官换台机器也能验证你的代码。
5. **`.gitignore` 掉 `data/`、`results/`、`.venv/`**，别把几百 MB 数据推上去。

---

## 7. 实测踩过的坑（按"害人程度"排序）

| # | 坑 | 症状 | 正确做法 |
|---|---|---|---|
| 1 | **`cat` 读二进制串口** | 帧长在 76~100 字节抖动，误判为变长协议 | pyserial / `stty raw` |
| 2 | **`T_IC` 方向写反** | 数值"看着正常"，但和 Kalibr 对不上，平移方向全错 | 明确声明约定 + 写逆矩阵自检 |
| 3 | **`square_size` 与实物不符** | 内参对、平移尺度整体错 | 打印后用尺子量，写进配置 |
| 4 | **只绕一个轴转** | AX=XB 数学上无解，解出来是噪声 | 绕三个轴分别转 |
| 5 | **纯旋转不平移** | `t_IC` 不可观测 | 一定要有平移激励 |
| 6 | **时间戳没对齐** | 外参旋转误差随手一抖就 1°+ | 互相关估 $\tau$，锁曝光 |
| 7 | **用标定数据算验证误差** | 误差看着很漂亮，实际偏乐观 | 留 20% 验证集 |
| 8 | **用 OpenCV 开海康工业相机** | `VideoCapture` 永远打不开 | 它不是 UVC，走 GenTL/MVS SDK |
| 9 | **指望磁力计** | yaw 恒漂 | 这块板磁力计恒 0，别用 |
| 10 | **相机与 IMU 松动** | 结果每次都不一样，且无法复现 | 用支架/热熔胶刚性固定，全程别碰 |
| 11 | **自动曝光** | 帧率抖动，时间戳不可靠，运动模糊 | `--lock-exposure` |
| 12 | **`BUFFERSIZE` 默认太大** | "读到帧的时刻"比曝光晚几十毫秒且不稳定 | 设 `CAP_PROP_BUFFERSIZE=1`（采集脚本已做） |

---

## 8. 学习资源

**必读（按优先级）**

1. **Zhang, Z. (2000). _A Flexible New Technique for Camera Calibration_.**
   TPAMI. —— 内参标定的原始论文，读懂它 C1 就通了。
2. **OpenCV 官方教程**：`Camera Calibration`、`Pose Estimation`、`solvePnP`。
3. **手眼标定**：Tsai & Lenz (1989)；Park & Martin (1994)。
   或者直接看 **Kalibr** 的文档和论文（Furgale et al., _Continuous-Time Batch
   Estimation for Visual-Inertial Calibration_）。
4. **Kalibr** —— 成熟工具，用来做"与成熟工具对比"这一项验证。
   注意它的 `T_cam_imu` 方向和我们的 `T_IC` **相反**。

**本仓库内**

- `~/calib_ws/doc/H7_IMU协议.md` —— 你这块 IMU 的完整协议与证据链
- `~/calib_ws/doc/相机取流诊断.md` —— 海康相机取流排查记录
- `~/calib_ws/README.md` —— 环境、格式、坐标系定义
- `~/calib_ws/calib/imu_h7.py` —— 可用的协议解析参考实现
- `~/calib_ws/tools/capture_h7.py` —— 可用的采集脚本

**RM 社区参考**（看别人怎么组织视觉代码，别抄标定实现）

- [RoboMaster 论坛开源区](https://bbs.robomaster.com/) —— 各队视觉/雷达算法开源

---

## 9. 答辩速查：8 个必答问题

验收时大概率会被问到，建议**先自己想一遍**：

1. 你的 `T_IC` 是哪个坐标系到哪个坐标系？为什么这么定义？
2. 相机内参的重投影误差是多少？多少算好？你的达标了吗？
3. 你怎么知道相机和 IMU 的时间戳是对齐的？偏移多少？怎么估的？
4. 如果我把 `square_size` 改成两倍，哪些输出会变？哪些不变？为什么？
5. 你的标定结果里，平移外参的模长是多少？和实物尺子量出来的对得上吗？
6. 手眼标定为什么需要绕多个轴运动？只绕一个轴会怎样？
7. 磁力计能不能帮你提升精度？为什么？
8. 如果给你一组新的数据，你怎么判断标定结果是可信的？

**能答完这 8 个问题，这道理就稳了。**

---

*最后一句：这道题真正的考点不是"你会不会用 OpenCV"，而是
**你能不能对自己产出的每一个数字负责**。*
